export const selectProfilePhoto = (state) => state.profile.user.avatar;
export const selectProfileName = (state) => state.profile.user.name;
export const selectProfileUser = (state) => state.profile.user;
export const selectAuth = (state) => state.profile.authed;
